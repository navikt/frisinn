import React from 'react';
import ErrorPage from '../../components/error-page/ErrorPage';

const GeneralErrorPage = () => {
    return <ErrorPage pageTitle={'Det oppstod en feil'} />;
};

export default GeneralErrorPage;
